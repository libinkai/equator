# 位运算符

## `&` 按位与

## `|` 按位或

## `~` 按位非

## `^` 按位异或

> 不进位加法（加法，忽略进位）

- `x^0=x`
- `x^1s=~x`
- `x^(~x)=1s`
- `x^x=0`
- `c=a^b => a^c=b,b^c=a` 交换两个数
- `a^b^c=a^(b^c)=(a^b)^c` 结合律

## `<<` 左移

## `>>` 右移

## `<<<` 无符号右移

# 指定位的位运算

- `x&(~0<<n)` 将x最右边的n位清零
- 

